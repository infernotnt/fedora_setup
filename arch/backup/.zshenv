export ZDOTDIR=$HOME/.config/zsh

# Disables the useless "less" command history file that is usualy put in the home directory
export LESSHISTFILE="-"

# Enables firefox on wayland
export MOZ_ENABLE_WAYLAND=1

#TODO: videti gde sta staviti, sta da ide u .profile sta u .zshenv itd... pogledaj otvorene tabove u firefox, i vidi cinjenicu da na githubu luke smihtu mozda stoji u .profile a u videu mu stoji u .zshenv
